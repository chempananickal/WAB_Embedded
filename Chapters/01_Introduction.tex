\chapter{Introduction}

\section{Background}

The octanol-water partition coefficient (\gls{logp}) measures how well a compound dissolves in octanol versus water.
It is a fundamental physicochemical property that quantifies the \gls{lipophilicity} of a molecule. 

\begin{displaycquote}{mm-pbsa-logp}
    The logarithm of partition coefficient (logP) between n-octanol and water is a critical property related to the physicochemical and physiological properties of a drug molecule, 
    such as absorption, distribution, metabolism, excretion and toxicity (ADMET) and target protein binding \cite{HUGHES20084872,waring2009defining,gleeson2008generation,johnson2009using}.
    Thus it is frequently used in describing the druggability of a pharmaceutical molecule. The first logP calculation method was developed by Fujita et al. in 1960s \cite{fujita1964new,iwasa1965substituent}. 
    Early on, the structural information of receptor is not easy to obtain, medicinal chemists frequently used quantitative structure-activity relationship (QSAR) to correlate the binding energy of a ligand with its physicochemical characteristics, 
    and logP was regarded as a key descriptor measuring a drug's hydrophobicity. 
    Nowadays, Lipinski's rule of five \cite{lipinski2001polar,lipinski2004lead}, published in the 1990s, has been widely used as simple rules to determine the druglikeness of a pharmaceutical molecule. 
    logP smaller than 5 is one of the four rules in Lipinski's rule of 5.
\end{displaycquote}

\section{Motivation}

To date, there have been many computational methods developed to predict \gls{logp} values, 
including aggregating the logp contributions of individual fragments \cite{logpfrag1, logpfrag2}, 
free energy based calculations \cite{mm-pbsa-logp}, and more recently, 
employing machine learning based methods to predict \gls{logp} values based on a precomputed set of input features \cite{logp-rf,logp_ml,logp-prediction} .

Fragment based \gls{logp} calculations involve having a lookup table of individual fragments, and a method of constructing a molecular graph and finding the fragments within it. 
Both this and the free energy based calculations require a non-trivial amount of computational resources, and are therefore infeasible to be implemented as a near real-time prediction method on a microcontroller.

The machine learning based approaches could potentially be feasible, 
if a neural network could be trained to predict \gls{logp} values and translated into a microcontroller compatible format using LiteRT \cite{litert} (formerly TensorFlow Lite).

The reason for exploring the ability to predict \gls{logp} on a microcontroller is to enable real-time, on-site predictions in various applications, such as environmental monitoring or portable drug discovery tools.
By offloading the prediction task away from a host computer and onto a microcontroller, one can potentially reduce latency in systems that require immediate response,
such as a microfluidic dispensing device that needs to make decisions based on the \gls{logp} of a compound in real-time.

Since the molecular \gls{fingerprint} is a bit vector that could be encoded as a \gls{qrcode} (see Figure \ref{fig:qr_code_example} for an example), the microcontroller could also be used in scenarios where a user can scan said code printed on the sample in a laboratory 
or field setting with a portable device and receive an immediate prediction of the \gls{logp} value,
and if this approach seems feasible and accurate enough, it could be extended to predicting other properties as well, without needing to access a cloud-based service or a powerful computer.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Code/Results/qr_codes/aspirin.png}
    \caption{A QR Code encoding the ECFP4 fingerprint of aspirin. The \gls{smiles} string was initially canonicalized via RDKit \cite{rdkit}, and the fingerprint was generated using the Morgan algorithm \cite{morgan} with a radius of 2 and a bit vector length of 2048. The resulting bit vector was converted to a byte stream then encoded into a QR code using the qrcode library in Python.}
    \label{fig:qr_code_example}
\end{figure}

Also, by decoupling the \gls{fingerprint} generation (a deterministic process on canonical \gls{smiles} strings) and the prediction (a non-deterministic process owing to the nature of machine learning models),
and by binding the prediction to a specific piece of hardware (a "black box", as it were), one can make the entire system more modular and easier to debug. 
As an added bonus, the host computer wouldn't need to have any machine learning libraries installed.

\section{Research Question}

The main research question of this project is:
How accurately can a quantized machine learning model predict \gls{logp} values based on its \acrshort{ECFP4} \gls{fingerprint} when deployed on an \gls{esp32} microcontroller?

\section{Previous Work}

To date, there have been multiple efforts to train machine learning models to serve as \gls{logp} predictors,
such as the work by Ognichenko et al. \cite{logp-rf} who trained an \gls{rf} model on the 2D Simplex Representation of Molecular Structures (SiRMS) descriptors \cite{sirms} of 10973 compounds, 
and the work by Baik\'et\'e et al. \cite{logp_ml} who trained a variety of machine learning models (\gls{rf}, \gls{xgboost}, among others) on 623 molecular descriptors generated by the RDKit \cite{rdkit} and Mordred \cite{mordred} packages for a dataset of 1,117 compounds.
As of the time of writing, there have been no known efforts to deploy a machine learning based \gls{logp} predictor on a microcontroller.

This current project aims to build upon the efforts of G\'omez-Bombarelli et al. \cite{zinc20source}, who created a neural network based \gls{logp} predictor
on a subset of the \gls{ZINC20} dataset \cite{zinc} as part of creating a variational autoencoder for molecular generation. 
Their dataset serves as the basis for training the model in this project, as it provides a large enough set of drug-like compounds with valid \gls{smiles} strings.
This dataset was also chosen because it was used to train a neural network based \gls{logp} predictor, which is the type of model that can be deployed on a microcontroller using LiteRT (formerly \gls{tflite}) \cite{litert}.